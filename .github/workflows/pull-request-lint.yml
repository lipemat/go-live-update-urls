##############################
# Test and lint files in a pull request.
#
# @version 0.0.3
#
##############################

name: Test & Lint Files

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

jobs:
  lint-test:
    uses: lipemat/public-actions/.github/workflows/pull-request-lint-plugin.yml@master
